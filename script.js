const lightEl = document.getElementById("lightmode");

const h1El = document.querySelector("h1");

const buttonEl = document.createElement("button");
buttonEl.textContent = "Toggle Dark Mode";
lightEl.appendChild(buttonEl);

